counts = []

def createVote(countsArray):
    # We'll expand this to customize the amount of options,
    # but for testing, keep it at 2
    for count in countsArray:
        count = 0

def takeVote(vote):
    vote = int(vote) - 1
    counts[vote] = counts[vote] + 1
    

